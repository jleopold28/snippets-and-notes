package pangram

import "strings"

// Not finishing since discovered Go has such poor intrinsics for doing this

// Pangram determines if phrase uses every letter at least once
func Pangram(phrase string) bool {
	lowerPhrase := strings.ToLower(phrase)
	var letters []char

	for _, letter := range lowerPhrase {

	}
}
