package sieve

// go cannot remove an element from an array without onerous tricks

// Sieve sieve of erastosthenes to find all primes from 2 up to given number
func Sieve(limit int) []int {
	var nums [limit - 1]int
	for i := 2; i < limit; i++ {
		num[i-2] = i
	}
	for _, num := range nums {

	}
}
