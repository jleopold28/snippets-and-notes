---
version: 5

hierarchy:
- name: FQDN
  path: "fqdn/%{trusted.certname}.yaml"

- name: Location
  path: "location/%{facts.location}.yaml"

- name: Common
  path: common.yaml

defaults:
  data_hash: yaml_data
  datadir: hieradata
